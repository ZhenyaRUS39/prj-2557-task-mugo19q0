# Plan: prj-2557-task-mugo19q0

**Project:** `prj-2557-task-mugo19q0`  
**Task ID:** `task-mugo19q0`  
**Repo:** `prj-2557-task-mugo19q0`  

## Summary

План: утилита grep для JSON на Python — CLI с поддержкой ключей, значений, путей и регулярок. 4 этапа: структура+парсер, поисковый движок, форматирование вывода, тесты+README.

## Subtasks

### 1. Инициализация репозитория и CLI-скелет

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать структуру проекта: pyproject.toml/requirements.txt, src/jsongrep/__init__.py, src/jsongrep/cli.py с argparse (аргументы: файл/STDIN, PATTERN, --key/--value/--path, --ignore-case, --regex, --output-format text/json/count). Точка входа через __main__.py или console_scripts.
- **Test plan:** python -m jsongrep --help показывает все аргументы; пустой вызов падает с кодом 2; pip install -e . устанавливает пакет без ошибок.

### 2. Реализовать загрузку и поисковый движок

- **ID:** `sub-2`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Функции load_json (поддержка .json и .jsonl по расширению, обработка ошибок парсинга с понятным сообщением в stderr и exit 1). Рекурсивный обход структуры с формированием JSONPath вида $.a.b[0].c. Поиск совпадений по подстроке или regex в ключах/значениях/путях согласно флагам. Поддержка вложенных объектов и массивов любой глубины.
- **Test plan:** На тестовом fixture (3-уровневый JSON с массивом и несовпадающим элементом) утилита находит ровно ожидаемые пути; exit 0 при совпадениях, exit 1 при отсутствии (по аналогии с grep).
- **Dependencies:** `sub-1`

### 3. Форматирование вывода и режимы отображения

- **ID:** `sub-3`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Реализовать форматтеры: text (по умолчанию) — путь:значение или путь:ключ=значение; json — выдаёт массив объектов {path, key, value}; count — количество совпадений. Поддержка --no-filename при чтении из STDIN. Цветной вывод через colorama/простой CSI при --color=auto/always/never (детект TTY).
- **Test plan:** На одном и том же вводе: --output-format text/json/count выдают ожидаемые представления; piping в другой grep работает; --color=never отключает ANSI при перенаправлении в файл.
- **Dependencies:** `sub-2`

### 4. Тесты, документация и примеры

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Pytest-покрытие движка и CLI (минимум 15 кейсов: подстрока, regex, --key, --value, --path, игнорирование регистра, битый JSON, .jsonl, пустой файл, exit codes). README.md с примерами: jsongrep.py data.json 'foo', jsongrep.py data.json 'user.*id' --regex --key, пайп из curl. Запуск CI-готового линта (ruff или flake8).
- **Test plan:** pytest -v проходит все кейсы, coverage >= 80% по src/jsongrep; README содержит секции install/usage/examples; примеры из README воспроизводятся на приложенном sample.json.
- **Dependencies:** `sub-3`


---
*Generated by Hermes Orchestrator*
